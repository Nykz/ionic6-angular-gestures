import { PressDirective } from './press.directive';

describe('PressDirective', () => {
  it('should create an instance', () => {
    const directive = new PressDirective();
    expect(directive).toBeTruthy();
  });
});
